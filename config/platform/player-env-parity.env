# DEPRECATED — do not edit this file.
#
# Board-specific parity lives in:
#   player-env-parity.pi4.env  — Pi 4 / BCM2711 (1024×3, governor v1)
#   player-env-parity.pi5.env  — Pi 5 / BCM2712 (128×2, governor v2, provisional)
#
# apply-player-env-parity.sh selects from /proc/device-tree/model.
# Governor keys are applied all-or-nothing per board file — never partial merge.
