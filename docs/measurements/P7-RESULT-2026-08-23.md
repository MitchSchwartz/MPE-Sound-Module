# P7 — overclock diagnostic result

*Completed: 2026-08-23 (America/Toronto)*  
*Pi: `raspberrypi2.local` · git `1c165b9` · artifacts `~/plan-p7-20260823-145330`*

---

## Verdict

**Comparison invalid — H6 inconclusive.** OC @2000 MHz produced plausible loaded DSP (~34–53% `dsp_p99`). Baseline @1800 MHz produced ~5–8% for the same patches and voice counts — far below reference-suite floors (~38–57%) and **inconsistent with a loaded confirm cell**. Overclocking cannot show *lower* DSP if the baseline never reached loaded state.

Pre-registered **alarm** fired: higher clock → higher reported DSP (opposite of compute-bound prediction).

**Do not use this run to close H6.** Re-run baseline (or full P7) with verified voice hold before V12 close-out cites clock scaling.

---

## Execution

| phase | clock | runs | xruns | throttled |
|---|---|---:|---:|---|
| Baseline | ~1800 MHz | 9 | 0 | 0x0 |
| Overclock | ~2000 MHz (`frequency(48)=2000478464`) | 9 | 0 | 0x0 |

Config reverted at end of OC phase. **Second reboot required** to return to stock ~1800 MHz.

Poly governor was off for P7; re-enable after final reboot for V12.

---

## Numbers

### Baseline @ ~1800 MHz

| patch | voices | run | dsp_p99 | dsp_max |
|---|---:|---|---:|---:|
| Crystals | 3 | 1–3 | 5.60–5.64 | 5.66–5.80 |
| Cloud Horn | 5 | 1–3 | 7.63–7.66 | 7.78–7.97 |
| Duduk | 3 | 1–3 | 5.58–5.63 | 5.69–5.74 |

### Overclock @ ~2000 MHz

| patch | voices | run | dsp_p99 | dsp_max |
|---|---:|---|---:|---:|
| Crystals | 3 | 1–3 | 33.56–34.58 | 34.14–35.76 |
| Cloud Horn | 5 | 1–3 | 52.76–53.26 | 54.82–55.13 |
| Duduk | 3 | 1–3 | 33.87–34.20 | 34.82–35.22 |

Cloud Horn @5: baseline median ~7.6% vs OC ~52.8% — **~7× increase at higher clock**, not the ~−10% expected if compute-bound.

---

## Confound (likely)

Same harness, same patch path, same voice count in provenance — but loaded state differs:

- Baseline Cloud Horn run1: `dsp_median=7.5%` from second 1 (idle-scale)
- OC Cloud Horn run1: `dsp_median=51.4%` from second 1 (loaded-scale)

Jack ALSA device also differed after reboot (`hw:2` baseline vs `hw:1` OC) — card reorder, probably not the DSP driver, but worth noting in artifact.

**Leading hypothesis:** baseline confirm cells did not sustain voice hold (or Surge was not in loaded poly state) despite patch OSC load. OC half after reboot did.

---

## Post-P7 on Pi

After **reboot #2** (config already reverted):

```bash
sudo ./scripts/pi-overclock-config.sh status   # expect ~1800, throttled=0x0
sudo systemctl enable --now surge-poly-governor.service
```

---

## Close-out hook

H6 row: **inconclusive (instrument failure on baseline half)** — not “clock not binding” until a valid A/B exists.

---

## Baseline re-run (2026-08-23 evening)

*Artifacts: `~/plan-p7-baseline-rerun-20260823-201311/` · Pi git `801771b`*

### Verdict

**Baseline half valid** for Cloud Horn and Crystals — loaded plausibility holds. Prior morning run (~7% Cloud Horn) is superseded for H6 **baseline** numbers; **full H6 A/B still needs a paired OC re-run** in the same harness generation (morning OC @2000 MHz remains on disk but was not re-measured this session).

Preflight before baseline: `measure-soak-preflight.sh` Cloud Horn @5 @1024×2 — **pass** (~109 s).

### Baseline @ ~1800 MHz (3×45 s confirm cells)

| patch | voices | dsp_p99 (runs) | dsp_median (run1, from log) |
|-------|-------:|----------------|----------------------------:|
| Crystals | 3 | 37.02, 37.33, 37.35 | (confirm summary) |
| Cloud Horn | 5 | 34.69, 35.34, 34.23 | **33.13%** |
| Duduk | 3 | 11.15, 11.00, 11.20 | ~11% (light patch; not ~7% unload) |

All runs: **0 xruns**, `throttled=0x0`, clock ~1800 MHz.

### H6 note (not closed)

With valid baseline Cloud Horn **~34% p99** vs prior OC half **~53% p99** @2000 MHz, reported DSP **rises** with clock — still opposite naive compute-bound headroom direction. Do not close H6 without a **paired** OC phase on this baseline artifact or a single `measure-plan-p7.sh` full run.

`surge-poly-governor.service` re-enabled after baseline.
